[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: PostgreSQL + pgvector 통합 테스트 (docker-compose.test.yml 필요)
    external: 외부 API 실연동 테스트 (환경변수 필요, CI에서 skip)
