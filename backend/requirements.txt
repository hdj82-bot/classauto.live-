# Core
fastapi>=0.136.1,<1.0.0
# uvicorn floor 0.30→0.48 (2026-06-05): 워크트리 전체 테스트 그린(0.49.0 검증).
# 프로덕션 start command(--workers 4)·옵션 시그니처 무변경. 상한 1.0 미만 유지.
uvicorn[standard]>=0.48.0,<1.0.0
pydantic>=2.13.4,<3.0.0
pydantic-settings>=2.14.1,<3.0.0
python-multipart>=0.0.30,<1.0.0

# Database
sqlalchemy>=2.0.50,<3.0.0
alembic>=1.13.0,<2.0.0
asyncpg>=0.29.0,<1.0.0
psycopg2-binary>=2.9.12,<3.0.0
pgvector>=0.4.2,<1.0.0

# Redis / Celery
redis>=5.0.0,<7.0.0
celery[redis]>=5.4.0,<6.0.0

# Auth
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
google-auth>=2.50.0,<3.0.0
google-auth-oauthlib>=1.2.0,<2.0.0

# HTTP
httpx>=0.27.0,<1.0.0
# NOTE(N8): tenacity 는 미사용. app/core/retry.py 가 의도적으로 stdlib-only 구현으로
# 동일 시그니처를 제공한다. 향후 tenacity 도입을 결정하면 이 줄을 다시 추가하면 된다.

# AI / ML
anthropic>=0.102.0,<1.0.0
# images.edit 의 input_fidelity(교수 사진 정체성 보존; Photo Avatar v0.2)는 1.96.0+ 에서 지원.
# 2026-06-05: floor 를 2.38.0 으로 끌어올림 — 운영/개발이 이미 2.x 설치본을 쓰므로
# 소스와 핀을 일치시킨다(1.x 회귀 설치 방지). images.edit·embeddings.create·AsyncOpenAI·
# BadRequestError 시그니처는 1.x→2.x 무변경(워크트리 전체 테스트 846 passed, 2.41.0 검증
# — docs/verification/00-index.md). 상한은 3.0 미만 유지. (공유 의존성 — 병렬 브랜치 merge 시 주의)
openai>=2.38.0,<3.0.0

# Pipeline
python-pptx>=1.0.2,<2.0.0
Pillow>=12.2.0,<13.0.0
deepl>=1.30.0,<2.0.0
google-cloud-translate>=3.18.0,<4.0.0
google-cloud-texttospeech>=2.36.0,<3.0.0

# Storage
boto3>=1.34.0,<2.0.0

# Payment
stripe>=15.1.0,<16.0.0

# Monitoring
sentry-sdk[fastapi]>=2.60.0,<3.0.0
prometheus-client>=0.21.0,<1.0.0
